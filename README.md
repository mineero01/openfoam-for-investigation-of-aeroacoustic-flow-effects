Incompressible perfect gas, steady-state (and temporal). Mesh, exact flow values and running scripts removed. systemTemporal includes files for transient sim.
Depending on the case, a good steady-state solution or an averaged temporal solution can be used as background fields for a LNS solution in COMSOL.
